Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(?:deploy|includes|plantillas|sql|storage|vendor)(?:/|$) - [F,L,NC]
</IfModule>

<FilesMatch "^(?:\.env(?:\..*)?|composer\.(?:json|lock)|Dockerfile|README\.md)$">
    Require all denied
</FilesMatch>
